export const Title = (props) => {
    return (
        <>
            <div className="text-center">
                <h1 className="text-white font-semibold text-3xl title py-3">{props.title}</h1>
            </div>
        </>
    )
}